# 安全政策

[English](SECURITY_EN.md)

## 支持版本

当前项目仍处于 `0.x` 阶段。安全修复优先发布到最新的 `main` 分支和最新 crates.io 版本；旧版本是否回补取决于影响范围和维护成本。

## 报告漏洞

如果你发现安全问题，请不要先公开披露。请通过 GitHub 私密漏洞报告或仓库维护者公开资料中提供的私下联系方式报告，并尽量包含：

- 受影响版本或提交；
- 操作系统和目标平台；
- 复现步骤或最小输入；
- 潜在影响，例如崩溃、越界读取、拒绝服务或不安全 FFI 使用；
- 你是否已经验证过最新 `main` 分支。

## 响应预期

维护者会尽力在收到报告后尽快确认影响范围，并在修复可用后协调披露。修复可能包括 Rust crate 更新、C ABI 文档更新、头文件重新生成和 GitHub Release 说明。

## 安全边界

Fjiffyldg 主要处理本地文件读取、行索引和 C ABI 调用。C API 调用方必须遵守文档中的指针、长度、缓冲区生命周期和只读 mmap 约束；违反这些约束可能导致未定义行为或进程崩溃。
